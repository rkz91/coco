---
description: "Show all unread emails with count and top 20 list. No arguments needed."
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion
---

# /email-unread — Show unread emails

Invoke the Skill tool: `email` with args `unread`.
