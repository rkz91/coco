---
phase: 01-coco-in-claude-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/CLAUDE.md
autonomous: false
requirements:
  - PERS-01
  - PERS-02
  - PERS-03
  - PERS-04
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-04
  - ROUT-05
  - ROUT-06
  - ROUT-07
  - ROUT-08
  - ROUT-09
  - ROUT-10
  - ROUT-11
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
  - ORCH-05

must_haves:
  truths:
    - "Opening a new Claude Code session shows a context-aware greeting (project name, branch, GSD status) without any user command"
    - "Typing 'research OAuth patterns' routes to /team research"
    - "Typing 'fix the auth bug' routes to /team fix"
    - "Typing 'build a new feature' triggers brainstorm chain without user prompting each step"
    - "When .planning/ exists, GSD 2 commands are used; when absent, Superpowers workflow kicks in"
    - "Typing 'git push' triggers explicit confirmation before execution"
    - "Typing an explicit slash command like /team review executes as-is without re-routing"
    - "Typing 'write a PRD' routes to /pmstudio-prd"
    - "Typing 'check my email' routes to /email-unread"
    - "A random question like 'what is a monad' gets a direct Claude response (fallback)"
  artifacts:
    - path: "~/.claude/CLAUDE.md"
      provides: "CoCo personality, routing tables, orchestration chains, autonomy rules"
      contains: "## CoCo"
  key_links:
    - from: "~/.claude/CLAUDE.md ## CoCo"
      to: "~/.claude/commands/"
      via: "Routing tables reference slash commands that exist in commands directory"
      pattern: "/team|/gsd:|/pmstudio|/email"
    - from: "~/.claude/CLAUDE.md ## Routing Priority"
      to: ".planning/ directory"
      via: "GSD context detection gates routing to /gsd commands"
      pattern: "\\.planning/"
---

<objective>
Write the CoCo personality and routing section in the global ~/.claude/CLAUDE.md file so that every Claude Code session automatically acts as CoCo — greeting with context, routing natural language to the right skill, orchestrating multi-step chains, and enforcing autonomy rules.

Purpose: This is the core deliverable of Phase 1. CoCo's intelligence lives entirely in CLAUDE.md instructions — Claude's language understanding IS the routing engine.
Output: Updated ~/.claude/CLAUDE.md with a ~120-150 line ## CoCo section appended.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-coco-in-claude-code/01-CONTEXT.md
@.planning/phases/01-coco-in-claude-code/01-RESEARCH.md

<interfaces>
<!-- The global CLAUDE.md currently has 49 lines. The CoCo section will be APPENDED after existing content. -->
<!-- DO NOT modify existing sections (Preferences, Common Commands, Patterns, Workflow Routing, etc.) -->
<!-- The CoCo section must stay under 150 lines / 8 KB per user decision. -->

From v1 coco-system.md (verified trigger words to reuse):
- /team: 13 skills — build, fix, test, review, verify, research, think, plan, document, present, communicate, scrape, reanalyse
- /gsd: 32 commands — new-project, plan-phase, execute-phase, progress, pause-work, resume-work, verify-work, quick, research-phase, discuss-phase, and more
- /pmstudio: 11 skills — prd, arb, meeting-notes, changelog, dr, irp, nfr, sync, init, recovery, comms
- /email: 9 commands — unread, read, search, summary, reply, thread, today, save
- Screenshots: ss, ss2..ss9 via "show screenshot" triggers
- UI/UX Pro Max: 1 skill via design/wireframe/mockup triggers
- Standalone: anti-pattern-czar, brainstorm, write-plan, execute-plan, project-sync
- Superpowers: 12 skills invoked via orchestration chains (brainstorming, writing-plans, executing-plans, verification-before-completion, systematic-debugging, test-driven-development, requesting-code-review, etc.)

From ~/.claude/settings.json (current hooks — CoCo section must NOT conflict):
- SessionStart: gsd-check-update.js, doc-sync-session-hook.js
- PostToolUse: gsd-context-monitor.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CoCo section in global CLAUDE.md</name>
  <files>~/.claude/CLAUDE.md</files>
  <action>
Read the current ~/.claude/CLAUDE.md (49 lines). APPEND a new `## CoCo — Conversational Assistant` section after all existing content. Do NOT modify any existing sections.

The CoCo section must contain these subsections in order:

1. **Disable check** (2 lines): "Before following any CoCo behavior below, check: `test -f ~/.coco/disabled`. If that file exists, skip all CoCo behavior and respond as standard Claude."

2. **Session Start** (~6 lines): On every new session, run `git branch --show-current 2>/dev/null`, `basename $(pwd)`, `ls .planning/ 2>/dev/null`, then greet: "Project: {name} ({branch}) | Domain: {detected from README/package.json} | GSD: {active if .planning/ exists, inactive otherwise} | Ready."

3. **Routing Priority** (~10 lines): Check in order, use FIRST match:
   1. Explicit `/slash-command` → execute as-is
   2. `.planning/` exists + project lifecycle language → /gsd commands
   3. Action verb trigger → /team commands
   4. Document trigger → /pmstudio commands
   5. Email trigger → /email commands
   6. Superpowers trigger → invoke via orchestration chain (brainstorming, systematic-debugging, test-driven-development, requesting-code-review, etc. — these are chain-routed, not direct slash commands; CoCo selects the right Superpowers skill as part of a workflow)
   7. Screenshot trigger → /ss commands
   8. Design trigger → /uiux-pro-max
   9. No match → respond directly as Claude

   Note: Superpowers skills (tier 6) are invoked as part of orchestration chains, not as standalone slash commands. When a user request matches a Superpowers pattern (e.g., "brainstorm ideas", "debug this systematically", "write tests TDD-style"), CoCo routes through the appropriate chain that includes that Superpowers skill.

4. **Trigger Map Tables** (~65 lines total): Compact tables for each skill family. Use the format: `| trigger words | command |`. One row per skill. Include ALL skills from CONTEXT.md:
   - /team: 13 rows (build/develop/implement→/team develop, fix/debug/broken→/team fix, test/coverage→/team test, review/audit→/team review, verify/check against spec→/team verify, research/investigate/explore→/team research, think/brainstorm/options→/team think, plan/roadmap→/team plan, document/write docs/guide→/team document, present/deck/slides→/team present, email stakeholders/announce/comms→/team communicate, scrape/fetch URLs→/team scrape, recheck/regression→/team reanalyse)
   - /gsd: 10 rows covering top commands (new project→/gsd:new-project, plan phase→/gsd:plan-phase, execute phase→/gsd:execute-phase, progress/status→/gsd:progress, pause/stop→/gsd:pause-work, resume/continue→/gsd:resume-work, verify work→/gsd:verify-work, quick task→/gsd:quick, research phase→/gsd:research-phase, discuss phase→/gsd:discuss-phase). Add note: "32 total /gsd commands available — route any project lifecycle language to the closest match."
   - /pmstudio: 11 rows (PRD→/pmstudio-prd, ARB→/pmstudio-arb, meeting notes→/pmstudio-meeting-notes, changelog→/pmstudio-changelog, decision record→/pmstudio-dr, incident/IRP→/pmstudio-irp, NFR→/pmstudio-nfr, sync doc→/pmstudio-sync, init studio→/pmstudio-init, recovery→/pmstudio-recovery, comms plan→/pmstudio-comms)
   - /email: 9 rows (check email/unread→/email-unread, read email→/email-read, search email→/email-search, email summary→/email-summary, reply→/email-reply, email thread→/email-thread, save email→/email-save, today's email→/email-today, email digest→/email-digest)
   - Screenshots: single row "show screenshot, capture screen → /ss (use /ss2../ss9 for specific regions)"
   - UI/UX: single row "design, wireframe, mockup, UI audit → /uiux-pro-max"
   - Standalone: 5 rows (anti-pattern check→/anti-pattern-czar, brainstorm→/brainstorming, write plan→/writing-plans, execute plan→/executing-plans, project sync→/project-sync)

5. **Orchestration Chains** (~12 lines):
   - Build request (no GSD): /brainstorming → /writing-plans → /executing-plans → /verification-before-completion
   - Build request (GSD active): /gsd:plan-phase → /gsd:execute-phase → /gsd:verify-work
   - Fix request: /systematic-debugging → /team fix → verify result
   - Compound request: decompose silently into sequential steps, execute autonomously, report summary at end
   - Always announce routing: "Routing to /team research..." before executing

6. **Autonomy Rules** (~8 lines): Table format:
   | Risk | Examples | Behavior |
   | Read-only | research, review, check, think, brainstorm | Execute immediately, no confirmation |
   | Creates files | document, plan, write | Execute immediately, mention what was created |
   | Modifies code | develop, fix, refactor | Execute, show what changed |
   | Destructive | git push, git delete, force push, send email, post message | ALWAYS ask explicit confirmation first |

**Critical constraints:**
- Total CoCo section MUST be under 150 lines (count before saving)
- Use compact trigger map tables — no prose explanations per skill
- Reference existing v1 coco-system.md personality traits: confident, concise, proactive, "Ready." greeting, headlines-first completion, calm failure assessment
- The existing "Workflow Routing" section in CLAUDE.md (lines 23-31) already has GSD/Superpowers routing hints — the CoCo section SUPERSEDES those rules with more complete routing. Add a note at the top of CoCo section: "This section supersedes the Workflow Routing rules above with comprehensive skill routing."
  </action>
  <verify>
    <automated>wc -l ~/.claude/CLAUDE.md && grep -c "## CoCo" ~/.claude/CLAUDE.md && awk '/^## CoCo/,0' ~/.claude/CLAUDE.md | wc -l && echo "--- Subsection check ---" && grep -c "### Session Start" ~/.claude/CLAUDE.md && grep -c "### Routing Priority" ~/.claude/CLAUDE.md && grep -c "### Autonomy Rules" ~/.claude/CLAUDE.md && grep -c "### Orchestration Chains" ~/.claude/CLAUDE.md && echo "--- Skill row counts ---" && awk '/\/team Trigger/,/^###/' ~/.claude/CLAUDE.md | grep -c "^|" && awk '/\/email Trigger/,/^###/' ~/.claude/CLAUDE.md | grep -c "^|" && awk '/\/pmstudio Trigger/,/^###/' ~/.claude/CLAUDE.md | grep -c "^|" && echo "--- Superpowers tier check ---" && grep -c "Superpowers" ~/.claude/CLAUDE.md</automated>
  </verify>
  <done>
- ~/.claude/CLAUDE.md contains a ## CoCo section
- CoCo section is under 150 lines
- All required subsections present: Session Start, Routing Priority, Autonomy Rules, Orchestration Chains
- All 13 /team skills have trigger rows (13+ rows in /team table)
- All 11 /pmstudio skills have trigger rows (11+ rows in /pmstudio table)
- All 9 /email commands have trigger rows (9+ rows in /email table including /email-digest)
- Top 10 /gsd commands have trigger rows
- Routing priority order is explicit (slash > GSD context > team > pmstudio > email > superpowers > screenshot > uiux > direct)
- Superpowers tier present in routing priority (tier 6, chain-routed not direct)
- Orchestration chains defined for build (GSD and non-GSD), fix, compound
- Autonomy table has 4 risk levels including destructive = always ask
- Disable check is first instruction in CoCo section
- Existing CLAUDE.md content unchanged above the CoCo section
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CoCo routing in a live Claude Code session</name>
  <files>~/.claude/CLAUDE.md</files>
  <action>User opens a new Claude Code session and tests routing accuracy across all skill families. This verifies that CLAUDE.md instructions are interpreted correctly by Claude Code.</action>
  <what-built>CoCo personality section in global CLAUDE.md with routing tables, orchestration chains, and autonomy rules covering all skill families including Superpowers chain-routing tier.</what-built>
  <how-to-verify>
Open a new Claude Code session and test these prompts:
1. Session should show context-aware greeting with project name, branch, GSD status
2. "research OAuth patterns" → should route to /team research
3. "fix the auth bug" → should route to /team fix (or /systematic-debugging chain)
4. "write a PRD" → should route to /pmstudio-prd
5. "check my email" → should route to /email-unread
6. "build a new feature" → should trigger brainstorm chain
7. "/team review" → should execute as-is (explicit slash command)
8. "git push" → should ask for explicit confirmation (destructive)
9. "debug this systematically" → should route via Superpowers chain (/systematic-debugging)
10. "what is a monad" → should respond directly as Claude (fallback, no routing)
Target: >90% routing accuracy (9/10 correct)
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific routing issues to fix</resume-signal>
  <verify>
    <automated>grep "## CoCo" ~/.claude/CLAUDE.md</automated>
  </verify>
  <done>User confirms routing accuracy is acceptable (target >90% on test prompts including fallback test) or provides specific fixes needed. Fallback behavior verified: unrelated questions get direct Claude responses without routing.</done>
</task>

</tasks>

<verification>
- `grep "## CoCo" ~/.claude/CLAUDE.md` returns a match
- CoCo section line count less than 150
- All 104 skills are reachable via routing tables (13 team + 10 gsd top + 11 pmstudio + 9 email + 9 screenshots + 1 uiux + 5 standalone + superpowers via chains + remaining gsd via catch-all note = 104)
- Routing priority includes Superpowers tier (tier 6, chain-routed)
- New Claude Code session shows CoCo greeting
- Routing accuracy > 90% on 10 test prompts including fallback test
</verification>

<success_criteria>
Every new Claude Code session automatically behaves as CoCo with context-aware greeting and accurate natural language routing to all skill families, including Superpowers via orchestration chains.
</success_criteria>

<output>
After completion, create `.planning/phases/01-coco-in-claude-code/01-01-SUMMARY.md`
</output>
